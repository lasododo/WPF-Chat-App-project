<UserControl x:Class="PrismCalculatorFollowingTutorialProject.MessageSender"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrismCalculatorFollowingTutorialProject"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="500">
    <Grid Background="IndianRed">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="" Padding="10" x:Name="TextBoxer"></TextBox>
            <Button Grid.Column="1" Width="100" Height="50" HorizontalAlignment="Right" Background="Azure" Margin="10 0 0 0" Click="Button_Click"> Send</Button>
        </Grid>
    </Grid>
</UserControl>
